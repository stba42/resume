[~ if (r.projects && r.projects.length ) { ~]
%-------------------------------------------------------------------------------
%	SECTION TITLE
%-------------------------------------------------------------------------------
\cvsection{Private Projekte}

\begin{cvparagraph}
Bei der folgenden Auflistung handelt es sich um Projekte, die vollständig neben der Vollzeittätigkeit entstanden sind.
Diese wurden komplett eigenverantwortlich konzipiert, entwickelt und auf Server- bzw. Cloudinfrastruktur deployt und betrieben.
\end{cvparagraph}

%-------------------------------------------------------------------------------
%	CONTENT
%-------------------------------------------------------------------------------
\begin{cventries}
[~ _.each( r.projects, function(project) { ~]
%---------------------------------------------------------
  \cventry
    {[[ project.description ]] } % Job title
    {[[ project.title ]] 	\textnormal{\textit{(\href{[[ project.url ]]}{[[ project.url ]]})}}}
    { }
    {[[ h.dateRange( project ) ]]} % Date(s)
    {
      [[ project.summary ]] \endgraf
\textit{[~ print( project.keywords.join(', ') ) ~]}
    }

[~ }); ~]
%---------------------------------------------------------
\end{cventries}
[~ } ~]
